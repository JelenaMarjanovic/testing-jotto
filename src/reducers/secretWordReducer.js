/**
 * @function secretWordReducer
 * @param {array} state - A secret word.
 * @param {object} action - Action to be reduced.
 * @returns {array} - New secretWord state.
 */
const secretWord = (state = '', action) => {
  return state;
};

export default secretWord;
